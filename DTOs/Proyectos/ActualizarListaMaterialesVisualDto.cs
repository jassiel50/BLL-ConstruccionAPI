namespace BLL_ConstruccionAPI.DTOs.Proyectos;

// Texto libre, sin validación de formato — es solo una lista visual, no datos reales de inventario.
public class ActualizarListaMaterialesVisualDto
{
    public string? Contenido { get; set; }
}
